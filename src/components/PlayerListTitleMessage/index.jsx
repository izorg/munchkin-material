import React from 'react';
import { FormattedMessage } from 'react-intl';

const PlayerListTitleMessage = () => <FormattedMessage id="player.list.title" defaultMessage="Munchkins" />;

export default PlayerListTitleMessage;
